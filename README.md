# 🛒 Online Store API

Интернет-магазин с REST API и интерактивной документацией Swagger.  
Проект выполнен в рамках практических занятий 1-6 по дисциплине **"Фронтенд и бэкенд разработка"**.

## 🚀 Установка и запуск

1. **Клонируйте репозиторий**
```bash
git clone https://github.com/D0n1s1m0/online-store.git
cd online-store
```

2. **Установите зависимости**
```bash
npm install
```

3. **Скомпилируйте SCSS в CSS**
```bash
npm run sass
```

4. **Запустите сервер**
```bash
# Только сервер
npm start

# Или сервер + автоматическая компиляция SCSS
npm run dev:all
```

5. **Откройте в браузере**
- Магазин: http://localhost:3001
- API: http://localhost:3001/api/products
- Swagger документация: http://localhost:3001/api-docs

## 📜 Доступные npm скрипты

| Команда | Описание |
|---------|----------|
| `npm start` | Запуск сервера |
| `npm run dev` | Запуск сервера с автоматической перезагрузкой (nodemon) |
| `npm run sass` | Компиляция SCSS в CSS (с отслеживанием изменений) |
| `npm run dev:all` | Запуск сервера и SCSS компилятора одновременно |
| `npm run kill` | Принудительное завершение процесса на порту 3001 |
| `npm run reset` | Перезапуск сервера |
